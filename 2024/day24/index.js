import { input } from './input.js';

const parseString = (input) => {};

const main = (data) => {
  let result = 0;

  return result;
};

const result = main(parseString(input));

console.log(result);
